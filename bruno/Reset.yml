info:
  name: Reset
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:{{PORT}}/reset
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
