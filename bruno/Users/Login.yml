info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:{{PORT}}/users/register
  body:
    type: json
    data: |-
      {
        "username": "wad",
        "password": "amazing",
        "repeatPassword": "amazing"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
