info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:{{PORT}}/users/login
  body:
    type: json
    data: |-
      {
        "username": "wad",
        "password": "amazing"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
