info:
  name: Login
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:{{PORT}}/users/login
  body:
    type: json
    data: |-
      {
        "username": "joe69",
        "password": "passw123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
