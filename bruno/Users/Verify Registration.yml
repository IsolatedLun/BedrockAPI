info:
  name: Verify Registration
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:{{PORT}}/users/verify-registration
  body:
    type: json
    data: |-
      {
        "token": "d6f89cce-9672-4c88-861e-1f7ee9125de5",
        "otp": "876031"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
