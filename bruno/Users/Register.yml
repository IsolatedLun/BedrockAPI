info:
  name: Register
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:{{PORT}}/users/register
  body:
    type: json
    data: |-
      {
        "username": "joe1232",
        "email": "herojoe210@gmail.com",
        "password": "passw123",
        "repeatPassword": "passw123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
