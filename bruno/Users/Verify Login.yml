info:
  name: Verify Login
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:{{PORT}}/users/verify-login
  body:
    type: json
    data: |-
      {
        "token": "8f9f599e-324a-4a89-baff-abeacd2f6574",
        "otp": "339793" 
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
