info:
  name: Verify Login
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:{{PORT}}/users/login-otp
  body:
    type: json
    data: |-
      {
        "token": "",
        "otp": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
