info:
  name: View All Users [Insecure]
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:{{PORT}}/users/all
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
