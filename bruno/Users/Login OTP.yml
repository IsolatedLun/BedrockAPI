info:
  name: Login OTP
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:{{PORT}}/users/login-otp
  body:
    type: json
    data: |-
      {
        "email": "herojoe210@gmail.com",
        "otp": "664618"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
