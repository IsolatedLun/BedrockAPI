info:
  name: Search
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:{{PORT}}/notes/search
  body:
    type: json
    data: |-
      {
        "title": "?"
      }
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
