info:
  name: Delete Note
  type: http
  seq: 2

http:
  method: DELETE
  url: http://localhost:{{PORT}}/notes/delete/:id
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "title": "yo",
        "text": "<h3>3rd note<h3>"
      }
  auth:
    type: bearer
    token: "{{jwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
