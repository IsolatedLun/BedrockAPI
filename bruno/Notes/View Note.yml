info:
  name: View Note
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:{{PORT}}/notes/:id
  params:
    - name: id
      value: "2"
      type: path
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
