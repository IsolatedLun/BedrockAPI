info:
  name: View All Notes [Insecure]
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:{{PORT}}/notes/all
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
