info:
  name: Edit Note
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:{{PORT}}/notes/edit/:id
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "title": "yo123",
        "text": "### crazy67"
      }
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
