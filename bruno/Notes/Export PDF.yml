info:
  name: Export PDF
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:{{PORT}}/notes/export-pdf/:id
  params:
    - name: id
      value: "17"
      type: path
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
